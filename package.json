{
  "name": "trendvault",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "type-check": "turbo type-check",
    "db:migrate": "pnpm --filter @trendvault/api exec prisma migrate dev",
    "db:seed": "pnpm --filter @trendvault/api exec prisma db seed",
    "db:studio": "pnpm --filter @trendvault/api exec prisma studio",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f docker/docker-compose.yml down"
  },
  "devDependencies": {
    "prettier": "^3.5",
    "turbo": "^2.4"
  },
  "packageManager": "pnpm@9.15.0"
}
